# Быстрый старт: BSL (1C:Предприятие) в Serena

Serena теперь поддерживает язык BSL (Built-in Scripting Language) платформы 1C:Предприятие.

## Требования

1. **Python 3.11 или выше** (для Serena)
   ```bash
   python --version
   ```

2. **uv** (менеджер пакетов для Serena)
   ```bash
   uv --version
   ```

## Настройка MCP

```{
  "mcpServers": {
    "serena": {
      "type": "stdio",
      "command": "C:\\Users\\User\\.local\\bin\\uv.exe",
      "args": [
        "run",
        "--directory",
        "D:\\app\\Serena", *укажите путь где расположены исходники serena*
        "serena-mcp-server",
        "--context",
        "ide",
        "--project",
        "${workspaceFolder}"
      ],
      "env": {},
      "alwaysAllow": ["check_onboarding_performed", "delete_memory", "edit_memory", "find_file", "find_referencing_symbols", "find_symbol", "get_symbols_overview", "initial_instructions", "insert_after_symbol", "insert_before_symbol", "list_dir", "list_memories", "onboarding", "read_memory", "rename_symbol", "replace_content", "replace_symbol_body", "search_for_pattern", "summarize_changes", "think_about_collected_information", "think_about_task_adherence", "think_about_whether_you_are_done", "write_memory"],
      "disabled": false
    }
  }
}
```

## Использование в IDE

При открытии IDE автоматически стартует рабочая область Serena.

Настройки находятся в папке пользователя C:\Users\<Имя пользователя>>\.serena\serena_config.yml

Примеры настроек с описанием в файле ___serena_config.yml:
  ```
   - web_dashboard_open_on_launch: true: открытие страницы Serena dashboard (http://127.0.0.1:24282/dashboard/index.html).
   - ls_specific_settings:
      bsl:
         enable_hash_prefiltering: True #предварительная фильтрация файлов по хешу перед индексацией
         file_read_parallelism: 500 #количество параллельных операций чтения файлов
  ```

Serena автоматически определит `.bsl` файлы и предоставит:

- 🔍 Поиск функций и процедур
- 📍 Переход к определению
- 🔗 Поиск использований
- 🔧 Быстрые исправления
- 📐 Форматирование кода
- ✏️ Переименование символов

## Поддерживаемые расширения файлов

- `.bsl` - модули BSL (стандартное расширение)

## Структура проекта 1С

Типичная структура проекта, которая хорошо работает с Serena:

```
МояКонфигурация/
├── ОбщиеМодули/
│   ├── Утилиты.bsl
│   ├── ОбработкаДанных.bsl
│   └── Валидация.bsl
├── Справочники/
│   └── Товары.bsl
├── Документы/
│   └── Счет.bsl
└── .serena/
    └── project.yml
```

## Производительность

### Для больших конфигураций

1. **Исключите папки сборки** в `.gitignore`:
   ```
   build/
   ```

## Дополнительные ресурсы

- [Документация Serena](https://oraios.github.io/serena/)

## Примеры использования

### Пример 1: Поиск функции

В IDE:
```
Найди функцию РассчитатьСумму в проекте
```

Serena найдёт все определения и использования функции.

### Пример 2: Рефакторинг

```
Переименуй процедуру ОбработатьДанные в ОбработатьЭлементы во всех файлах
```

Serena выполнит переименование с учётом всех ссылок.

### Пример 3: Документация

```
Покажи документацию для функции ПриветствоватьПользователя
```

Serena покажет комментарии и сигнатуру функции.

## Поддержка

**Готово!** Теперь вы можете использовать Serena с проектами 1C:Предприятие! 🚀