// Модуль утилитарных функций для работы со строками и коллекциями
// Вспомогательный модуль для тестирования BSL Language Server

#Область ПрограммныйИнтерфейс

// Функция преобразования строки в верхний регистр
//
// Параметры:
//  Строка - Строка - исходная строка
//
// Возвращаемое значение:
//  Строка - строка в верхнем регистре
//
Функция ПривестиКВерхнемуРегистру(Строка) Экспорт
    
    Возврат ВРег(Строка);
    
КонецФункции

// Функция преобразования строки в нижний регистр
//
// Параметры:
//  Строка - Строка - исходная строка
//
// Возвращаемое значение:
//  Строка - строка в нижнем регистре
//
Функция ПривестиКНижнемуРегистру(Строка) Экспорт
    
    Возврат НРег(Строка);
    
КонецФункции

// Функция удаления пробелов из строки
//
// Параметры:
//  Строка - Строка - исходная строка
//
// Возвращаемое значение:
//  Строка - строка без пробелов по краям
//
Функция УдалитьПробелы(Строка) Экспорт
    
    Возврат СокрЛП(Строка);
    
КонецФункции

// Функция проверки наличия элемента в массиве
//
// Параметры:
//  Массив - Массив - массив для поиска
//  Элемент - Произвольный - искомый элемент
//
// Возвращаемое значение:
//  Булево - истина, если элемент найден
//
Функция СодержитЭлемент(Массив, Элемент) Экспорт
    
    Для Каждого ТекущийЭлемент Из Массив Цикл
        Если ТекущийЭлемент = Элемент Тогда
            Возврат Истина;
        КонецЕсли;
    КонецЦикла;
    
    Возврат Ложь;
    
КонецФункции

// Процедура логирования сообщения
//
// Параметры:
//  Сообщение - Строка - текст сообщения
//  Уровень - Строка - уровень важности (Информация, Предупреждение, Ошибка)
//
Процедура ЗаписатьВЛог(Сообщение, Уровень = "Информация") Экспорт
    
    ПолноеСообщение = СтрШаблон("[%1] %2: %3", ТекущаяДата(), Уровень, Сообщение);
    Сообщить(ПолноеСообщение);
    
КонецПроцедуры

// Функция проверки валидности email
//
// Параметры:
//  Email - Строка - email адрес для проверки
//
// Возвращаемое значение:
//  Булево - истина, если email валиден
//
Функция ПроверитьEmail(Email) Экспорт
    
    Если ПустаяСтрока(Email) Тогда
        Возврат Ложь;
    КонецЕсли;
    
    Если СтрНайти(Email, "@") = 0 Тогда
        Возврат Ложь;
    КонецЕсли;
    
    Если СтрНайти(Email, ".") = 0 Тогда
        Возврат Ложь;
    КонецЕсли;
    
    Возврат Истина;
    
КонецФункции

// Функция проверки, является ли строка числом
//
// Параметры:
//  Строка - Строка - проверяемая строка
//
// Возвращаемое значение:
//  Булево - истина, если строка содержит число
//
Функция ЭтоЧисло(Строка) Экспорт
    
    Попытка
        Число(Строка);
        Возврат Истина;
    Исключение
        Возврат Ложь;
    КонецПопытки;
    
КонецФункции

// Функция создания массива из диапазона чисел
//
// Параметры:
//  Начало - Число - начало диапазона
//  Конец - Число - конец диапазона
//
// Возвращаемое значение:
//  Массив из Число - массив чисел от Начало до Конец
//
Функция СоздатьДиапазон(Начало, Конец) Экспорт
    
    МассивЧисел = Новый Массив;
    
    Для Индекс = Начало По Конец Цикл
        МассивЧисел.Добавить(Индекс);
    КонецЦикла;
    
    Возврат МассивЧисел;
    
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Служебная функция форматирования даты
//
// Параметры:
//  Дата - Дата - форматируемая дата
//
// Возвращаемое значение:
//  Строка - отформатированная дата
//
Функция ФорматироватьДату(Дата)
    
    Возврат Формат(Дата, "ДЛФ=DD");
    
КонецФункции

#КонецОбласти












